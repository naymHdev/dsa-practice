function isAnagram(s: string, t: string): boolean {
    // এখানে তোমার কোড লিখবে
    
}

// Test cases
console.log(isAnagram("anagram", "nagaram")); // true
console.log(isAnagram("rat", "car"));         // falsebun run valid-anagram.ts